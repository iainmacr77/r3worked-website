Solid ink panel with the hard coral offset shadow (`6px 6px 0px`) — the signature "sticker" shadow used on the pricing panel and dashboard/device mockups.

```jsx
<DarkPanel>
  <p style={{color:'var(--r3-porcelain)'}}>Enquiry control room</p>
</DarkPanel>
```
