Plain white rounded card with a soft tight shadow — used for form fields, enquiry detail tiles, dispatch outcome tiles. Pass `tight` for smaller/denser instances (2-up grids).

```jsx
<SurfaceCard tight>
  <span className="type-body-sm">Name</span>
</SurfaceCard>
```
