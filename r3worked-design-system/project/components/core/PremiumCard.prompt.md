The frosted "premium-card" glass surface (blur + soft shadow) used for the3 step cards and other elevated light-mode content blocks. Set `hoverLift` to get the signature coral sticker-shadow + lift on hover.

```jsx
<PremiumCard hoverLift>
  <h3 className="type-h3">Website uplift</h3>
</PremiumCard>
```
